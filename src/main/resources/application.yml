endpoints:
   enabled: false
   info:
      enabled: true
   health:
      enabled: true
   shutdown:
      enabled: true
   beans:
      enabled: true
   dump:
      enabled: true
   metrics:
      enabled: true

security:
   user:
      name: admin
      password: secret

management:
   security:
      roles: SUPERUSER
   context-path: /manager
